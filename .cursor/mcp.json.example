{
  "mcpServers": {
    "hostinger-mcp": {
      "command": "npx",
      "args": ["hostinger-api-mcp@latest"],
      "env": {
        "API_TOKEN": "<your-hostinger-api-token>"
      }
    },
    "Sanity": {
      "url": "https://mcp.sanity.io",
      "headers": {
        "Authorization": "Bearer <your-sanity-token — run: npx sanity@latest mcp configure>"
      }
    }
  }
}
